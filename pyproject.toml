[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "taskwarrior-textual"
version = "0.1.0"
description = "Textual TUI for Taskwarrior with project planning and analysis"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "GPL-3.0-or-later" }
authors = [{ name = "Florian Maillar" }]
dependencies = ["textual>=0.70"]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.24",
  "pytest-cov>=5",
  "pytest-xdist>=3.6",
  "ruff>=0.6",
]

[project.scripts]
taskwarrior-textual = "taskwarrior_textual.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/taskwarrior_textual"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = [
  "--cov=taskwarrior_textual",
  "--cov-report=term-missing",
  "--cov-report=html",
  "--cov-branch",
]

[tool.coverage.run]
branch = true
source = ["taskwarrior_textual"]

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 95

[tool.ruff]
target-version = "py311"
line-length = 100
